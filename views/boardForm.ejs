<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>게시글 작성/수정</title>
</head>
<body>
  <%- include('partials/header') %>

  <% const isEdit = post && post._id; %>
  <h2><%= isEdit ? '글 수정하기' : '새 글 작성하기' %></h2>

  <form action="/board/<%= category %>/<%= isEdit ? post._id + '/edit' : 'new' %>" method="POST">
    <div>
      <label>제목:</label>
      <input type="text" name="title" value="<%= post.title || '' %>" required />
    </div>
    <div>
      <label>내용:</label><br/>
      <textarea name="content" rows="5" required><%= post.content || '' %></textarea>
    </div>
    <button type="submit"><%= isEdit ? '수정하기' : '등록하기' %></button>
  </form>

  <%- include('partials/footer') %>
</body>
</html>
