<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>게시글 상세</title>
</head>
<body>
  <%- include('partials/header') %>

  <h2><%= post.title %></h2>
  <p>
    작성자: <%= post.author.username %><br>
    작성일: <%= new Date(post.createdAt).toLocaleString() %>
  </p>
  <div>
    <%= post.content %>
  </div>

  <!-- 관리자 전용 수정/삭제 버튼 예시 -->
  <% if (session.isAdmin) { %>
    <form action="/board/<%= post.category %>/<%= post._id %>/edit" method="GET" style="display:inline;">
      <button type="submit">수정</button>
    </form>
    <form action="/board/<%= post.category %>/<%= post._id %>/delete" method="POST" style="display:inline;">
      <button type="submit">삭제</button>
    </form>
  <% } %>

  <%- include('partials/footer') %>
</body>
</html>
